apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minecraft-network.fullname" . }}-test-connection"
  labels:
    {{- include "minecraft-network.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minecraft-network.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
